def solution(n, path, order):
    answer = True
    # path.sort(key=lambda x:x[0])
    path_dict = dict()
    for i in range(len(path)):
        for j in range(2):
            if j == 0:


    return answer